package net.jeroenimoo0.qatalog;

import junit.framework.Assert;

import org.junit.Test;
import org.restlet.data.Header;
import org.restlet.representation.Representation;
import org.restlet.resource.ClientResource;
import org.restlet.util.Series;

public class ServerTest {
	
	@Test
	public void testSeverStartAndStop() {
		//Assert.assertTrue(QAtalog.start());
		//QAtalog.stop();
	}
	
	@Test
	public void test() throws Exception {
		/*
		Assert.assertTrue(QAtalog.start());
		
		ClientResource cr = new ClientResource("http://localhost:8080" + "/push/5673df205af26b2534d7f889");

		@SuppressWarnings("unchecked")
		Series<Header> headers = cr.getHeaders();
		headers.set("Content-Type", "application/json");
		
		Representation response = cr.post("{\"wins\":[{\"x\":240,\"y\":920,\"time\":420}]}");
		System.out.println(response.toString());
		
		QAtalog.stop();
		*/
	}
}
